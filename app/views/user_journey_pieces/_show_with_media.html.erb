
  <%= cl_image_tag piece.photo.key, height: 300, width: 390, crop: :fill %>
  <div class='container w-100'>
    <h1 class='container w-100 text-center'><%= piece.name %></h1>
    <h2 class='container w-100 text-center'><%= piece.artist %></h2>
    <p class='container w-100 text-center'><%= piece.creation_year %></p>
    <p class='container w-100 text-center'><%= piece.materials_techniques %></p>
  </div>

  <div class='container w-100'>
    <p><i class="fa-regular fa-lightbulb"></i> <strong>Réponses aux questions : </p></strong>
    <% piece.questions.each do |question| %>
      <strong><p><%= question.content %></p></strong>
      <p><%= question.right_answer_content %></p>
    <% end %>

    <p><i class="fa-regular fa-lightbulb"></i> <strong>Le saviez-vous ?</strong></p>

    <% piece.fun_facts.each do |fun_fact| %>
      <ul><li class='fun-facts'><%= fun_fact %></li></ul>
    <% end %>
  </div>

  <p class='media'><%= piece.media.html_safe %></p>

  <div class='d-flex justify-content-center my-2'>

    <%= link_to "Continuer ma visite", user_journey_path(user_journey), class: 'btn btn-filled' %>
  </div>
